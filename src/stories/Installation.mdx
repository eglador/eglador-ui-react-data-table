import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Installation" />

# Installation

Package installation, peer dependencies, and Tailwind v4 integration.

---

## 1. Install the package

```bash
npm install eglador-ui-react-data-table
```

```bash
yarn add eglador-ui-react-data-table
```

```bash
pnpm add eglador-ui-react-data-table
```

---

## 2. Peer dependencies

The following peer dependencies must be present in your project:

<table>
  <thead>
    <tr>
      <th align="left">Package</th>
      <th align="left">Version</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>react</code></td>
      <td><code>{">= 18"}</code></td>
    </tr>
    <tr>
      <td><code>react-dom</code></td>
      <td><code>{">= 18"}</code></td>
    </tr>
    <tr>
      <td><code>tailwindcss</code></td>
      <td><code>^4.0.0</code></td>
    </tr>
  </tbody>
</table>

These are typically already present in modern React projects (Next.js, Vite, Remix).

> **Zero runtime dependencies.** `clsx` and `tailwind-merge` are pre-bundled — nothing else is pulled into your app.

---

## 3. Add `@source` for Tailwind v4

Tailwind needs to scan classes inside the package. Add the `@source` directive to your global stylesheet:

```css
@import "tailwindcss";

@source "../node_modules/eglador-ui-react-data-table";
```

The `@source` path varies based on your CSS file location:

<table>
  <thead>
    <tr>
      <th align="left">Framework</th>
      <th align="left">CSS file</th>
      <th align="left"><code>@source</code> path</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Next.js (App Router)</td>
      <td><code>app/globals.css</code></td>
      <td><code>../node_modules/eglador-ui-react-data-table</code></td>
    </tr>
    <tr>
      <td>Next.js (<code>src/</code>)</td>
      <td><code>src/app/globals.css</code></td>
      <td><code>../../node_modules/eglador-ui-react-data-table</code></td>
    </tr>
    <tr>
      <td>Vite + React</td>
      <td><code>src/index.css</code></td>
      <td><code>../node_modules/eglador-ui-react-data-table</code></td>
    </tr>
  </tbody>
</table>

---

## 4. Server components note (Next.js)

The component is marked `"use client"` (it uses `useState` / `useEffect`). Place it inside a client component or after a `"use client"` directive in the file you're importing it into.

---

## 5. Verify

The component API will be finalized in the next milestone. See **Getting Started** once the implementation lands.

---

## Issues?

Open an [issue on GitHub](https://github.com/eglador/eglador-ui-react-data-table/issues).
